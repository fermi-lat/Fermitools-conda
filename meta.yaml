{% set name = "fermitools" %}
{% set version = "1.3.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  git_url: https://github.com/fermi-lat/ScienceTools.git

build:
  number: {{ environ.get('BUILD_NUMBER', 0)}}
  skip: true  # [win]


requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    #- {{ compiler('fortran') }}
    #- gcc 4.8.5 # [linux]  
    #- scons 3.0.3
    #- fermi-repoman
    #- root
    
  host:
    #- {{ compiler('c') }}
    #- {{ compiler('cxx') }}
    #- {{ compiler('fortran') }}
    - ape 2.9.1
    - aplpy
    - astropy
    - blas
    - clhep
    - cppunit
    - f2c
    - fermi-repoman
    - fftw
    - git
    - gsl
    - healpix_cxx
    - matplotlib
    - numpy >=1.17
    - openblas
    - pmw
    - readline
    - root 
    - scons 3.0.3
    - swig
    - wcslib
    - xerces-c

  run:
    - ape 2.9.1
    - aplpy
    - astropy
    - blas
    - clhep 
    - cppunit
    - f2c
    - fftw
    - fermitools-data >=0.16 # This must be updated with every model release
    - gsl
    - healpix_cxx
    - matplotlib
    - ncurses
    - numpy  >=1.17
    - openblas
    - pmw
    - pyyaml
    - readline
    - root
    - scipy
    - wcslib
    - xerces-c

test:
  imports:
    - UnbinnedAnalysis
    - pyLikelihood

  commands:
    # These tests inspect linkages and libraries. They are very slow, so comment them out
    # for a quicker turnaround when testing
    #- conda inspect linkages -p $PREFIX {{ name }}  # [not win]
    #- conda inspect objects -p $PREFIX {{ name }}  # [osx]

