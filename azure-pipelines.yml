# -*- mode: yaml -*-

trigger:
  branches:
    include:
      - master
      - cmake-update
  paths:
    exclude:
      - azure-pipelines.yml
      - conda_build_config.yaml
      - .travis.yml
      - .azure-pipelines
      - .ci_support
      - .scripts
  tags:
    exclude:
      - '*'
stages:
# - stage: conda_build
#   displayName: 'Conda Build Stage'
#   jobs:
#     - template: ./.azure-pipelines/azure-pipelines-linux.yml
#     - template: ./.azure-pipelines/azure-pipelines-osx.yml

- stage: misc
  displayName: 'Misc CI support'
  jobs:
    - template: ./.azure-pipelines/azure-pipelines-tag.yml
    # - template: ./.azure-pipelines/azure-pipelines-envs.yml
